# Leave unset to use the local SQLite file at artifacts/engagement360.sqlite (zero setup).
# For cloud Postgres (Neon / Supabase), set the connection string:
# DB_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/engagement360
